plugins {
    id "java"
    id "io.freefair.lombok" version "3.1.4"
}

repositories {
    mavenCentral()
}

dependencies {
    compile "org.slf4j:slf4j-api:1.+"
}

lombok {
    config['lombok.log.fieldName'] = 'LOG'
}
